'use client';

import React from 'react';
import GlobalFooter from './GlobalFooter';

const Footer: React.FC = () => {
  // Using the new global footer
  return <GlobalFooter />;
};

export default Footer;
